[project]
name = "ucm-deployer"
version = "0.1.0"
description = "UCM (Unified Cache Management) 一键部署工具：面向 vLLM / vLLM-Ascend / SGLang 推理集群的镜像构建、容器创建与推理服务部署"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
